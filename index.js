// Déclaration d'une fonction pour ajouter deux nombres
function additionner(a, b) {
    return a + b;
}

// Déclaration d'une fonction pour soustraire deux nombres
function soustraire(a, b) {
    return a - b;
}

// Utilisation des fonctions définies ci-dessus
const resultatAddition = additionner(5, 3);
const resultatSoustraction = soustraire(10, 4);
const test = soustraire(15, 4);
const test2 = soustraire(20, 4);

// Affichage des résultats dans la console
console.log("Résultat de l'addition :", resultatAddition);
console.log("Résultat de la soustraction :", resultatSoustraction);
